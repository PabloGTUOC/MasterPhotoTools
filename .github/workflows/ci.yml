name: CI

on:
  push:
  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always

jobs:
  test:
    name: Build, Test, and Lint
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      # exiftool is required for metadata *writing* (specification §2.6) and by
      # the test fixture generator. The webkit/gtk libraries are required to
      # build phototools-desktop (Tauri v2) on Linux; without them
      # `cargo build --workspace` cannot succeed. nasm builds mozjpeg's SIMD
      # kernels — without it the JPEG encoder still works but loses the
      # assembly paths the §9.1 performance target depends on.
      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            libimage-exiftool-perl \
            nasm \
            libwebkit2gtk-4.1-dev \
            libgtk-3-dev \
            libxdo-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev

      - name: Verify exiftool version
        run: |
          exiftool -ver
          # Build plan §3 requires exiftool >= 12
          test "$(exiftool -ver | cut -d. -f1)" -ge 12

      - name: Install stable toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Cache dependencies
        uses: Swatinem/rust-cache@v2

      - name: Check formatting
        run: cargo fmt --all --check

      - name: Clippy lints
        run: cargo clippy --workspace --all-targets -- -D warnings

      - name: Build workspace
        run: cargo build --workspace

      - name: Test workspace
        run: cargo test --workspace

      - name: Isolated core tests
        run: cargo test -p phototools-core

  frontend:
    name: Front end
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: |
            frontend/shared/package-lock.json
            frontend/web/package-lock.json
            frontend/desktop/package-lock.json

      # Both front ends consume the shared package: the client as compiled
      # output, and the views as source compiled into each application's own
      # build. It is installed and built first for that reason, and its install
      # also supplies the `vue` types the shared views are typechecked against.
      - name: Build the shared API client
        run: |
          npm --prefix frontend/shared ci
          npm --prefix frontend/shared run build

      - name: Install web dependencies
        run: npm --prefix frontend/web ci

      - name: Typecheck
        run: npm --prefix frontend/web run typecheck

      - name: Build
        run: npm --prefix frontend/web run build

      # Phase 6 acceptance: no view may reach the network directly.
      - name: Transport boundary
        run: npm --prefix frontend/web run check:transport

      # The desktop front end compiles the same shared views over a Tauri
      # transport, so it gets the same three checks. Both typechecks cover
      # frontend/shared/src/ui, each against its own client — a view that used
      # something only one transport offers fails the other build.
      - name: Desktop front end
        run: |
          npm --prefix frontend/desktop ci
          npm --prefix frontend/desktop run typecheck
          npm --prefix frontend/desktop run build
          npm --prefix frontend/desktop run check:transport

      # Phase 6 acceptance: layout at 390 px. Needs a browser, so it installs
      # one here rather than assuming the runner has it.
      - name: Install Chromium
        run: npx --prefix frontend/web playwright install --with-deps chromium

      - name: Layout at 390px
        run: npm --prefix frontend/web run check:layout

      # Phase 13 acceptance, and it asserts numbers rather than shapes: a change
      # that unwindows the review grid, or slows a filter past its budget, fails
      # here rather than on somebody's Mac.
      - name: Ingest UI — 400 shots, bulk resize, publish gate
        run: npm --prefix frontend/web run check:ingest

      - name: Upload layout screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: layout-390px
          path: frontend/web/layout-proof/
          if-no-files-found: ignore
