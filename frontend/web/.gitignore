# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Emitted beside the source config by an earlier tsconfig. Vite prefers
# vite.config.js over vite.config.ts, so a stale copy silently replaces the
# real configuration.
vite.config.js
vite.config.d.ts
